# growi-plugin-tracker

GROWIのMarkdownに、進捗バー・マイルストーン・カウンターを追加するscriptプラグイン。

## 記法

### 進捗バー

```
::progress[開発]{value=75 max=100 color=#2f6f4f}
```

- `value` / `max`: 現在値・最大値(省略時は `max=100`)
- `color`: バーの色(省略可、省略時はテーマの primary 色)
- `[開発]` の部分がラベルとして表示される

### マイルストーン

```
:::milestone{title="v2.0 リリース"}
- [x] 2026-01-10 要件定義
- [x] 2026-03-01 設計
- [ ] 2026-08-30 リリース
:::
```

中身は通常のGFMタスクリスト。チェック済み/全体の数から達成率を自動計算して表示する。
GROWIのページ上でチェックボックスをクリックすると(GROWI標準のタスクリスト編集機能により)
リアルタイムに達成率が更新される。

### カウンター(いいねボタン)

```
::counter[いいね]{id="likes-top-page"}
```

押すと「いいね」、もう一度押すと解除するトグルボタン。表示される数は**そのidに
いいねしている人数**(GROWIアカウント単位で1人1票までしかカウントされない —
連打しても自分の分は1票のまま)。

- `id`: サーバー(`growi-socialcredit-API` の `/counters/:id/like` `/unlike` エンドポイント)
  に保存するためのキー。同じ `id` を持つボタンは**誰が・どのブラウザで見ても同じ人数を
  共有する**。`id` を省略すると、サーバーには保存されない・押した人単独にしか見えない
  0/1のローカルトグルになる。
- 誰が押したかはGROWIのログインユーザー名(`/_api/v3/personal-setting/` から取得)で
  識別している。ログインしていない状態では押せない(ボタンが無効化される)。
- 保存先APIのURLは `src/config.ts` の `COUNTER_API_BASE_URL` にハードコードされている
  (このプラグインにはサーバー側コンポーネントが無く、管理画面から設定変更する仕組みが
  存在しないため)。フォークして別のAPIに向ける場合はここを書き換えてビルドし直すこと。

### 賛成/反対投票

```
::vote{id="indication-42"}
```

押すと賛成、もう一度別の方を押すと反対に切り替わる排他投票ボタン(いいねボタンと違い、
どちらか一方にしか投票できない)。表示される件数は**そのidに賛成/反対している人数**。

- `id`: 保存先APIのキー(`growi-socialcredit-API` の `/votes/:id` `POST /votes/:id`)。
  省略するとサーバーに保存されず、ボタンは無効化された状態になる
- `agree-label` / `disagree-label`: ボタンのラベル文字列(省略時は「賛成」「反対」)
- カウンターと同じくGROWIのログインユーザー名で1人1票を管理。未ログイン時はボタンが無効化される

```
::vote[このページの指摘に賛成/反対]{id="indication-42" agree-label="賛成" disagree-label="反対"}
```

### Wiki指摘一覧

```
:::wiki-gap-suggestions
:::
```

Wiki巡回機能(`growi-socialcredit-API`)が出した未解決(`open`)の指摘を一覧表示する。
中身は書かなくてよい(`:::`〜`:::`は空でOK)。トップページなど任意のページに置くだけで、
各指摘のタイトル(指摘ページへのリンク)・要約・賛成/反対ボタンがその場に並ぶ。

## セットアップ

```bash
pnpm install   # または yarn / npm
pnpm build     # dist/ に成果物を生成する。dist/ はビルド後に git commit する
```

GROWIはプラグインをGitHubリポジトリから直接取得し、`dist/manifest.json` を読み込む
(サーバー側でビルドはしない)。そのため **`dist/` はビルド後に必ずコミットしてpush**
すること。

## GROWIへの登録

1. このディレクトリをGitHubリポジトリとしてpush(リポジトリに `growi-plugin` トピックを
   付けておくと一覧に出やすくなる)
2. GROWI管理画面 → プラグイン → 「プラグインインストーラー」に、このリポジトリの
   GitHub URLを入力してインストール
3. インストール後、対象ページで上記の記法を使うと即座に反映される(プレビューにも反映)

## 実装メモ

- GROWIのMarkdownパイプラインには remark-directive が標準で組み込まれているため、
  このプラグイン自身は remark-directive を同梱していない
  (`::name{...}` 構文は既にディレクティブASTノードとしてパース済みの状態で渡ってくる)。
- サニタイズ(`hast-util-sanitize`)はタグ名のホワイトリスト方式で、サードパーティ
  プラグインからは拡張できない。そのため独自タグ名は使わず、既に許可されている
  `figure` / `data` タグ + 常に許可される `data-*` 属性に載せてディレクティブの情報を
  運び、`components.figure` / `components.data` を既存コンポーネントをラップする形で
  上書きしている(`data-gpt-widget` 属性が無い要素はそのまま素通しする)。
